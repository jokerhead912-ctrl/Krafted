{
  "name": "krafted",
  "version": "5.4.0",
  "private": true,
  "description": "Krafted PWA — modular dev build",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo 'Build done. Open src/index.html in browser for dev.'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
